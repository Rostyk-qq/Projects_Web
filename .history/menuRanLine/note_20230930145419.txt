will-change: transform; 
ми використовуємо для того шоб вказати браузеру на анімацію яка буде 
для продуктивності можна використовувати 


якшо ми використовуємо елемент наприклад пункт від menu 
і не вказужємо height width але нам потрібно в js використовувати його offsetHeight offsetTop 
потрібно для нього вказати display: block;

атрибут data-color-effect 
нагадування отримуємо через 